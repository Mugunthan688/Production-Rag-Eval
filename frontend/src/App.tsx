import { AppShell } from "./components/layout/AppShell";

/**
 * Marginalia — Research Intelligence
 *
 * Single-screen app, no router needed.
 * AppShell manages all layout and state.
 */
export default function App() {
  return <AppShell />;
}
